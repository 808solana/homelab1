services:
  hls-nginx:
    image: nginx:alpine
    container_name: hls-nginx
    restart: unless-stopped
    ports:
      - "8082:80"
    volumes:
      - ./stream:/usr/share/nginx/html/stream:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
